# SAM Analysis

Bulk analysis and structured extraction of wage rate and fringe benefit data from raw text files scraped from the SAM.gov API.

Files are stored in S3 and processed at scale on Databricks. The goal is to catalog format families, improve extraction coverage, and produce a clean structured dataset queryable by county.
